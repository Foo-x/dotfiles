#!/usr/bin/env sh

is_win_dir() {
  case $(pwd -P) in
  /mnt/*)
    true
    return
    ;;
  *)
    false
    return
    ;;
  esac
}

if is_win_dir && type git.exe > /dev/null 2>&1 && [ ${WSL_VERSION} = 2 ]; then
  git.exe "$@"
else
  /usr/bin/git "$@"
fi
